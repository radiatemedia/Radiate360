#!/usr/bin/env rake
# encoding: utf-8
require 'bundler/gem_tasks'

# Include RSpec rake tasks
require 'rspec/core/rake_task'
RSpec::Core::RakeTask.new

# Make our Rspec task the default
task :default => :spec
